# ===========================================
# LLM API Keys (at least one required)
# ===========================================

# OpenAI - Required if using gpt-4o-mini (default)
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic - Required if using Claude models
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# ===========================================
# CrewAI Configuration
# ===========================================

# Model to use for agents (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, claude-3-5-sonnet-20241022, etc.
CREWAI_MODEL=gpt-4o-mini

# ===========================================
# Server Configuration
# ===========================================

# Port for the API server (default: 8000)
PORT=8000
